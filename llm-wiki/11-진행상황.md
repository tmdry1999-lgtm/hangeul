# 진행 상황

새 채팅에서 이어서 작업할 때 이 문서를 먼저 읽는다. 최신 상태를 유지하도록 계속 갱신한다.

## 지금 당장 (2026-09-03 기준, 다음에 이어할 때 제일 먼저 할 일)

**Vercel 배포 완료.** 실제 배포 주소: **https://hangeul-phi.vercel.app/**. Supabase Authentication > URL Configuration의 Site URL/Redirect URLs에도 이 주소를 등록해서 구글 로그인 후 localhost로 튕기던 문제를 해결함(아래 "배포" 절 참고).

**UI 정리 커밋은 이미 push 완료됨.** 그 직후 같은 세션에서 문장(7단계)/짧은 문단(8단계)을 추가로 구현했고, 사용자가 실제로 검사를 돌려본 뒤 받은 피드백으로 재생 버튼과 단어별 오류 진단까지 추가함 (아래 "문장(7단계)/짧은 문단(8단계) 추가"와 "열 번째 라운드" 절 참고) — **이 부분들 전부 아직 push 전, 코드 리뷰도 전**이니 다음에 이어할 때 먼저 처리한다.

1. 문장/문단 단계 브라우저 실사용 테스트: 실제로 문장을 녹음해서 Azure 종합점수(PronScore)가 그럴듯하게 나오는지, 새로 추가한 재생 버튼과 단어별 오류 태그(단어 생략/불필요한 말 추가/단어 발음 오류)가 실제로 잘 뜨는지, 좁은 카드에서 글자가 잘 보이는지, 결과 화면·리포트에 이상 없는지 확인.
2. 위 확인 후 git push 전 습관(작업 내역 정리 + 코드 리뷰)을 거쳐 push.
3. push 후 배포 주소(https://hangeul-phi.vercel.app/)에서도 오늘 바뀐 화면들(학생 목록 표, 학급통계 해득수준 배지, 검사 결과의 리포트 버튼, 지도방안 번호, 문장/문단 단계)이 실제로 잘 보이는지 확인. (재생 버튼은 서버 저장 없이 브라우저 메모리에만 두는 방식이라, 배포 주소에서도 "방금 그 자리에서 녹음한 문항"만 재생되는 게 정상 — 이전 기록엔 안 뜨는 게 의도된 동작.)

## 목표

**일요일 발표**에서 실제로 구동해서 보여줄 것. "자모→음절→단어" 발음 지도 앱의 1~6단계(모음, 자음, 받침없는 단어, 이중모음, 이중모음 단어, 대표받침)를 각 단계별 샘플 문항 1~3개 정도로, 처음부터 끝까지 동작하는 프로토타입으로 만든다. 문항을 촘촘히 채우는 것보다 전 단계가 끊김 없이 동작하는 것이 우선이다.

## 확정된 결정

- **음성인식/발음판정**: Azure AI Speech의 발음 평가(Pronunciation Assessment) 사용 ([10번](10-음성인식-기술-선택.md) 참고). Web Speech API는 사용하지 않음(발음 오류를 놓치기 쉬움).
- **배포**: 발표 당일 외부에서 접속 가능해야 하므로 **Vercel에 배포**한다.
- **버전 관리**: 사용자가 컴퓨터 두 대로 작업하므로 **GitHub 저장소를 만들어 두 컴퓨터를 동기화**하고, Vercel을 이 저장소에 연결해 push할 때마다 자동 배포되게 한다 (CLI 직접 업로드 대신 이 방식으로 결정함, 2026-08-31). git push는 계속 [00번 가이드](00-프로젝트-에이전트-가이드.md)의 "큰 변경 전 확인" 규칙을 따른다 — 저장소 최초 생성/연결은 승인받았지만, 이후 개별 push도 매번 먼저 알린다.
- **API 키 보관**: Azure 키는 Vercel 서버리스 함수(`/api/get-token` 같은 엔드포인트)에서만 사용하고, 이 함수가 10분짜리 임시 토큰을 발급해 브라우저에 내려준다. 진짜 키는 Vercel 환경 변수에만 저장하고 코드에는 넣지 않는다.
- **개인정보**: 학생 이름·학번 등 식별정보는 음성 요청과 함께 절대 전송하지 않는다. 세션은 임시 라벨로만 구분한다.

## Azure 설정 (완료)

- 대학원 계정으로 Azure for Students 인증 → Speech 리소스 생성 완료.
- 리전은 원래 계획한 Korea Central이 학생 구독 정책상 막혀서(`RequestDisallowedByAzure`) **Japan East**로 변경 (한국어 인식 지원은 리전과 무관하므로 문제 없음).
- 가격 계층: **Standard S0**. Cost Management에 예산 알림 설정함.
- 키 1 / 지역(japaneast) / 엔드포인트를 받아 프로젝트 루트의 `.env.local`에 저장 완료 (`AZURE_SPEECH_KEY`, `AZURE_SPEECH_REGION`, git에는 안 올라감).

## 알려진 문제

- **`npx vercel dev`가 이 컴퓨터에서 안 됨.** 컴퓨터 이름 자체가 "3F교사연구실1"로 한글이라 Vercel CLI 내부에서 로그인/텔레메트리 요청을 만들 때 오류(`Cannot convert argument to a ByteString...`)가 남 (환경 버그, 컴퓨터 이름을 영문으로 바꾸고 재부팅해야 근본 해결됨 — 지금은 급하지 않아 보류).
  - **다만 이것이 최종 배포를 막지는 않는다.** 실제 배포는 GitHub 연결 + Vercel 대시보드 빌드로 이뤄지며 로컬 CLI 로그인이 필요 없다.
  - 로컬 개발 중 테스트용으로 `dev-server.mjs`(vercel 없이 순수 Node로 `index.html`과 `api/get-token.js`를 서빙)를 대신 사용 중. 실행: `node dev-server.mjs` → http://localhost:3000
- **(1차 시도, 원인 파악됨) 짧은 음절이 다른 소리로 인식되던 문제.** 처음엔 "아"만 말해도 이상해서 짧은 음절의 한계인 줄 알았는데, 사용자가 "고양이 나비"라고 말했는데도 "아 아, 100점"이 뜨는 걸 발견함 — 이건 단순 한계가 아니라, 브라우저 SDK의 `enableMiscue` 옵션이 실제로 작동하지 않는 **알려진 SDK 버그**였음 ([MS Q&A](https://learn.microsoft.com/en-us/answers/questions/1182046/enablemiscue-parameter-not-working-when-using-java) — enableMiscue를 true로 줘도 결과가 안 바뀐다는 동일 증상 보고, 마이크로소프트 담당자도 SDK 버그 가능성 인정). 이 버그가 있는 WebSocket 기반 SDK 경로(`SpeechRecognizer.recognizeOnceAsync`)를 버리고, **REST API로 직접 녹음 파일을 보내 채점하는 방식**으로 교체함 (아래 "완료된 작업" 참고). 아직 실사용 테스트로 재확인 전이라 완전히 해결됐는지는 다음 세션에서 검증 필요.

## 완료된 작업

1. 프로젝트 구조: `index.html`(화면 전체) + `api/get-token.js`(Azure 임시 토큰 발급 서버리스 함수) + `.gitignore`(`.env.local` 등 비밀 파일 제외) + `dev-server.mjs`(로컬 테스트용).
2. 1~6단계 샘플 문항(각 3개)과 화면 흐름(단계 선택 → 듣기 → 말하기 → 결과) 구현 완료.
   - 듣기: Azure 고품질 한국어 음성(`ko-KR-SunHiNeural`)으로 모범 발음 재생 (처음엔 브라우저 내장 TTS를 썼다가 발음 품질이 안 좋아서 교체함).
   - 말하기: 처음엔 Azure Speech SDK(`SpeechRecognizer`, 웹소켓)로 구현했으나, `enableMiscue` SDK 버그 + "다시 누르면 취소만 되고 채점이 안 됨" 문제 때문에 마이크를 직접 녹음(버튼 다시 누르면 녹음 종료) → 녹음 파일을 Azure REST 발음 평가 API로 전송해 채점하는 방식으로 교체함.
     - 1차 시도(MediaRecorder로 webm 녹음): REST 응답 구조를 잘못 읽어서(`NBest[0].PronunciationAssessment.AccuracyScore`가 아니라 `NBest[0].AccuracyScore`가 맞음) 오류 발생 → 고침.
     - 2차 시도: 구조는 맞았는데 이번엔 전부 0점/인식된 소리 없음으로 나옴. 원인으로 의심되는 것: Azure REST 문서는 오디오 포맷으로 `audio/wav`(PCM)와 `audio/ogg;codecs=opus`만 공식 지원한다고 명시하는데, 브라우저 MediaRecorder 기본 녹음 포맷인 `audio/webm;codecs=opus`는 이 목록에 없었음.
     - 3차 시도: MediaRecorder 대신 Web Audio API로 직접 PCM 샘플을 모아 16kHz mono WAV 파일을 만들어서 전송하도록 교체함 (공식 지원 포맷 그대로) → **인식 자체는 성공** (사용자가 콘솔 로그로 확인, RecognitionStatus: Success, DisplayText가 실제 발음대로 나옴).
     - 4차 수정(부분적): 최상위 `DisplayText`로 바꿔봤지만, 여전히 목표 글자 쪽으로 쏠려서 나옴("마차"→"아아", "건"→인식 안 됨). Pronunciation-Assessment 헤더에 참조 텍스트를 실어 보내면 **인식 결과 자체가 그 참조 텍스트 쪽으로 편향된다**는 게 원인으로 최종 확인됨 (Azure 공식 문서도 "정확한 인식 텍스트가 필요하면 먼저 일반 STT를 따로 호출하라"고 안내함).
     - 5차 수정: **같은 녹음 파일을 두 번 전송**하는 방식으로 바꿈 — ① `Pronunciation-Assessment` 헤더 없이 순수 인식(실제로 뭐라고 말했는지), ② 헤더를 넣어 발음 채점(점수만 사용). 화면에는 ①에서 얻은 진짜 인식 텍스트를 보여줌 → **인식 텍스트 표시는 정상 확인됨.**
     - 6차 수정(채점 로직 자체를 바꿈): 인식된 글자가 목표와 다른데도 Azure 점수가 100점으로 나오는 문제 발견 → **목표 글자를 정확히 맞혔을 때만 Azure 점수를 그대로 쓰고, 안 맞으면 우리가 직접 채점**하도록 바꿈. 한글을 초성/중성/종성으로 분해해서(`decomposeHangul`) 목표와 얼마나 겹치는지 유사도(0~1)를 계산(`jamoSimilarity`) → 2/3 이상 겹치면 "비슷하지만 틀림"(70점 미만, "발음이 틀렸어요" — 예: 막→망처럼 받침만 다름), 그보다 적으면 "아예 다른 소리"(20점 미만, "다시 발음해보세요").
     - 7차 수정: "나비"를 "가수"로 잘못 말해도 "비슷함"으로 분류되는 문제 발견 → 원인은 받침 없는 음절끼리는 "둘 다 받침 없음"이라는 이유만으로 유사도 점수를 자동으로 얻고 있었음(의미 없는 유사성). 받침이 둘 다 없을 때는 비교 대상에서 아예 빼도록 `jamoSimilarity` 가중치를 수정함.
     - 8차 수정(사용자 요청): 정확한 단어로 인식된 경우는 지금처럼 Azure 세부 점수를 계속 쓰되(무조건 100점으로 안 함 — 받침을 흐릿하게 발음하는 것도 잡아내는 게 원래 목적이라 유지하기로 함, 2026-08-31 확정), **100점이 아닐 때 왜 100점이 아닌지 이유를 같이 보여주도록** `explainLowScore` 함수 추가. Azure 응답의 `SNR`(소리 크기), `FluencyScore`(자연스러움), 음소별 `AccuracyScore` 중 가장 낮은 걸 보고 "소리가 작았어요" / "말이 끊겼어요" / "'ㄱ' 소리가 흐릿했어요" 식으로 이유를 표시.
   - **편의 기능 추가**: 듣기 화면에 "이전 문제" 버튼(첫 문항에서는 안 보임, 화살표 기호는 사용자 요청으로 뺌), 단계 목록 화면에 "📋 결과 확인" 버튼.
   - **결과 화면 구조 변경(사용자 요청)**: 단계에 들어갈 때마다 "시험 1회"로 묶어서(`state.sessions`) 기록. 결과 확인 화면은 세션별 평균 점수만 먼저 보여주고, 탭하면 문항별 점수 + **틀린 문항에만** 이유를 보여주는 상세 화면(`renderSessionDetail`)으로 이동.
   - **채점 기준 조정(사용자 요청 + 합의)**: 정확한 단어를 말했을 때 Azure 점수가 90점대여도 100점이 아니어서 혼란스럽다는 피드백 → 90점 이상은 사람 귀로 구별 안 되는 노이즈로 보고 **100점 처리**, 90점 미만(진짜 문제일 가능성이 높은 구간)만 실제 점수 + 이유를 보여주는 절충안 적용 (2026-08-31 확정).
   - `explainLowScore`에서 음소별 점수 추측 부분은 제거함 (필드명을 확신할 수 없고 실제로 이상한 문구가 나왔음) — 지금은 SNR(소리 크기)/FluencyScore(자연스러움)만 확인하고, 그 외엔 "받침이나 소리의 일부가 정확하지 않았어요"라는 일반적 문구를 보여줌.
   - 아직 사용자 재테스트 전.
   - **9차 수정(사용자 요청, 문항 다양화)**: 각 단계 문항이 항상 똑같은 3개였던 걸, 단계별로 10~18개짜리 단어 은행(`stages[i].items`)으로 늘리고, 단계에 들어갈 때마다 그중 무작위로 5개를 뽑아서(`shuffle` 함수) 그 세션에서 씀 (`state.sessionItems`). 07/08번 참고자료 원칙(의미단어+무의미단어 섞기, 대표받침 7개는 표기와 소리가 일치하는 것만) 반영해서 단어 목록 구성함.
   - **10차 수정(사용자 요청, 화면 통합)**: "듣기" 화면과 "말하기 연습" 화면이 따로 있어서 매번 "말하기 연습 →" 버튼을 눌러야 했던 게 번거롭다는 피드백 → **한 화면에 듣기 버튼 + 마이크 버튼 + 이전 문제 버튼을 모두 합침**(`renderListen`/`renderSpeak` → `renderPractice` 하나로 통합, 화면 이름도 `listen`/`speak` → `practice`로 통일).
   - **11차 수정(사용자 요청)**: "이전 문제" 버튼을 없애고 그 자리에 **"모르겠어요" 버튼**을 추가함 (`markDontKnow` 함수) — 학생이 모르는 문항을 눌러서 넘기면 그 문항이 세션 결과에 "모르겠다고 답함"으로 기록되어, 결과 확인 화면에서 선생님이 어떤 문항을 몰랐는지 확인할 수 있음. "단계 목록으로"는 왼쪽, "모르겠어요"는 오른쪽에 배치(`.nav-row` flex).
   - **12차 수정(사용자 요청, 디자인)**: 전체적으로 너무 둥글둥글해서 "바이브 코딩 느낌"이 난다는 피드백 → 카드/버튼의 `border-radius`를 대부분 4~8px로 줄이고, 마이크 버튼도 완전한 원(50%)에서 각진 사각형(8px)으로 바꿈.
   - **13차 수정(2026-09-02, 사용자 요청)**: 다른 컴퓨터로 이어서 첫 실전 테스트 중 "라"를 정확히 말했는데 "락"으로 잘못 인식되어 오답(67점) 처리되는 사례 발견. 사용자가 "Azure에 같은 녹음을 세 번 보내는 식으로 정확도를 높일 수 있냐"고 물어봄 → **같은 오디오를 여러 번 다시 보내는 건 Azure가 결정적으로 같은 답을 주기 때문에 효과가 없다고 판단**하고, 대신 이미 받는 응답 안의 정보를 더 활용하도록 바꿈: 순수 인식 응답의 1순위 후보만 보던 것을, Azure가 함께 주는 대체 후보 목록(NBest)까지 확인해서 그중 목표 글자와 정확히 일치하는 게 있으면 그걸 정답으로 인정하도록 `assessRecording` 로직을 수정함. 다만 이렇게 해도 한 음절만 따로 말하면 근본적으로 헷갈릴 수 있는 한계는 남아있어서, **오답 화면(다시 시도/다음 문제 버튼 아래)에 "잘 발음했어요 (선생님 확인)" 버튼을 추가**함 — 선생님이 옆에서 듣고 실제로는 맞았다고 판단하면 눌러서 그 문항을 정답(100점)으로 수동 정정하고 다음 문제로 넘어갈 수 있음.
   - 아직 사용자 재테스트 전 (특히 "잘 발음했어요" 버튼과 NBest 개선이 실제로 어떻게 동작하는지).
   - **14차 수정(2026-09-02, 사용자 요청, 점수 체계 세분화)**: 실전 테스트 중 "라"를 "락"으로 잘못 인식해 67점(비슷하지만 틀림)이 나온 걸 계기로, 사용자가 "자음 또는 모음 하나가 틀렸을 때 50점은 너무 높지 않냐"고 문제 제기함. 기존 로직은 초성/중성이 통째로 다른 경우(예: '가'→'나', 자음 단계의 핵심 오류)와 받침만 다른 경우(예: '막'→'망')를 구분하지 않고 하나의 유사도 숫자(jamoSimilarity)로 뭉뚱그려서, 완전히 다른 소리인데도 50점 안팎의 애매한 점수가 나오는 문제가 있었음. → **`compareSyllable`/`classifyMismatch` 함수를 새로 추가**해서 음절마다 '초성·중성 자체가 다름(diff)' vs '받침만 다름(jongDiff)'을 먼저 구분하도록 바꾸고, 점수 구간을 명확히 분리함: diff가 하나라도 있으면 0~29점("완전히 다른 소리"), 없고 jongDiff만 있으면 단어 내 정확한 음절 비율 기반으로 40~69점("받침만 다름", 받침 문제가 있으면 절대 70점 이상은 못 넘도록 상한 유지). 30~39점 구간은 두 등급이 숫자만 봐도 바로 구분되도록 일부러 비워둠. 함수 하나로 시뮬레이션해서 '가'→'나' 50→29점, '라'→'락' 67→40점, '고구마'→'고국마'(3음절 중 1음절만 받침오류) 67→69점으로 바뀌는 걸 확인함 (문법 검사 + node로 로직만 떼어내 시뮬레이션, 브라우저 실사용 재확인은 아직 못 함).
   - **주석 추가(2026-09-02, 사용자 요청)**: 코드를 공부하려는 목적으로 `index.html`, `api/get-token.js`, `dev-server.mjs`, `supabase/schema.sql` 전체에 함수/블록 단위 설명 주석을 달아둠 (로직 변경 없음, 백업과 diff로 확인함). 앞으로도 이 사용자의 코드는 학습 목적상 주석을 꼼꼼히 다는 것을 기본으로 함.

## 검토됨: 채점 등급(14차 수정)이 07/08번 참고자료의 지도 원리와 부합하는지

- 사용자 요청으로 [07번](07-발음지도-참고자료-요약.md)/[08번](08-찬찬한글-참고자료-요약.md) 문서를 다시 읽고 14차 수정(diff/jongDiff 등급 분리)을 대조 검토함.
- 결론: 부합함. "소리를 지도하는 순서: 초성 → 종성(받침), 받침 없는 글자 → 받침 있는 글자"(07번)와 "모음을 자음보다 먼저 가르친다 - 모음이 자음보다도 더 기초"(08번) 원리에 따라, 초성/중성 오류(diff)를 받침만 다른 오류(jongDiff)보다 더 엄격하게(0~29점 vs 40~69점) 채점하는 지금 방식이 "받침은 단계적으로 익혀가는 중인 스킬, 초성·중성은 이미 다져놨어야 할 기초"라는 위계와 맞음.
- **나중에 반드시 다시 봐야 할 것**: 07번 문서는 "음운변동(연음·비음화·구개음화·ㅎ탈락)"을 **받침가족 단계 이후**에 반영할 예정이라고 명시함. 지금 6단계(대표받침) 단어는 표기와 소리가 일치하는 것만 골라둬서 문제 없지만, 나중에 받침가족·겹받침 단계를 추가해서 음운변동이 있는 단어(예: "국물"이 실제로는 "궁물"로 소리 나는 게 정답)를 다루게 되면, 지금의 jongDiff 채점 로직이 **이 정상적인 발음 변화까지 오답으로 잘못 채점**하게 된다. 그 단계를 만들 때 채점 로직을 반드시 함께 손볼 것.
- (참고, 지금 당장 할 필요는 없음) 08번 문서는 모음이 자음보다도 더 기초라고 설명하는데, 지금은 초성 오류와 중성 오류를 diff 하나로 묶어서 똑같이 채점한다. 필요하다고 판단되면 모음 오류를 자음 오류보다 한 단계 더 낮은 점수로 나누는 세분화도 가능하나, 지금 세분화만으로도 충분해 보여 보류함.

## 문장(7단계)/짧은 문단(8단계) 추가 (2026-09-03, 결정 및 구현 완료)

- 이전엔 "1~6단계가 완전히 안정된 뒤에 착수하자"고 보류해뒀던 항목 (아래는 그 논의 기록). 오늘 사용자가 "문장을 만들어보자"고 요청하면서 다시 착수함.
- **채점 방식 결정**: 문장은 자연스러운 억양·연음(예: "손이"가 "소니"로 들리는 것)이 정상이라, 1~6단계의 "목표 글자와 정확히 일치해야 Azure 세부 점수를 신뢰하고, 안 맞으면 초성/중성/받침을 직접 비교해서 채점"하는 방식을 그대로 쓰면 오히려 정상 발화를 오답으로 채점하게 됨. 에이전트가 3가지 안(Azure 종합점수 신뢰 / 단어 단위 비교 / 기존 방식 그대로)을 제시했고, **사용자가 "Azure 종합점수를 신뢰하는 방식으로 일단 시작"을 선택함**. 1~6단계 로직은 전혀 건드리지 않음.
- **구현**:
  - `stages` 배열에 `7. 문장`(짧은 문장 8개)과 `8. 짧은 문단`(2문장짜리 문단 5개) 추가. 두 단계 모두 `compositeScoring: true` 플래그를 붙임 (예시 문장/문단은 우선 채워 넣은 샘플이라, 실제 수업에 맞게 나중에 내용을 바꾸거나 늘릴 수 있음 - `stages` 배열의 문자열만 수정하면 됨).
  - `assessRecording`에서 `stages[state.stageIdx].compositeScoring`이 true면, 기존의 "정확히 일치 확인 → 안 맞으면 자모 비교" 분기를 타지 않고 Azure의 종합 발음 점수(`best.PronScore`, 없으면 `AccuracyScore`로 대체)를 그대로 채점 점수로 씀.
  - `analyzeError`도 문항 텍스트에 띄어쓰기가 있으면(=문장/문단) 음절 단위로 정확한 위치를 하나하나 비교하는 대신(문장 중간에 음절 하나만 밀려도 뒤가 전부 어긋난 것처럼 잘못 진단될 위험이 있음) `'유창성/발음 문제'`라는 뭉뚱그린 태그 하나만 붙이도록 분기 추가. `ERROR_TAG_ADVICE`/`STAGE_ADVICE`에도 7·8단계용 지도 문구를 추가함.
  - **"한글 해득" 판정 기준은 그대로 6단계 기준으로 고정**: `achievementTier`가 전체 `stages` 배열 길이가 아니라 새로 만든 `CORE_LITERACY_STAGE_COUNT`(=6) 상수만 봄. "한글 해득"이라는 용어 자체가 기초 문식성(자모~대표받침)을 가리키고, 문장·문단은 그 이후의 읽기 유창성 단계라서 해득 판정에 넣지 않기로 함 (배열 길이에 그대로 의존했다면, 이 단계를 추가하는 순간 학생들이 전부 미해득/보충으로 떨어지는 회귀가 생겼을 것 - node 시뮬레이션으로 6단계만 통과하면 7·8단계 상태와 무관하게 여전히 "해득"으로 나오는 것을 확인함).
  - `renderPractice`의 목표 글자 표시 글자 크기(`.target-text`, 원래 64px 고정)가 문장/문단처럼 글자 수가 많으면 좁은 카드(480px) 밖으로 넘치므로, 글자 수에 따라 30px(문장)/22px(문단)로 줄어드는 `.long`/`.paragraph` 클래스를 추가함.
- **논의 기록(과거)**: 사용자가 문장 단계 추가를 제안 → 에이전트가 "문장은 지금 채점 로직이 안 맞으니 1~6단계가 완전히 안정된 뒤에 착수하자"고 제안해서 보류했었음 ([09번 문서](09-앱-설계-개요.md)에도 원래 "프로토타입이 잘 작동하는 걸 확인한 뒤에 추가"라고 남겨둔 항목).
- node로 8단계 구성에서도 `achievementTier`가 기존과 동일하게 동작하는지, `node --check`로 스크립트 문법 오류가 없는지 확인함. **브라우저 실사용 재확인 필요** — 특히 문장/문단을 실제로 녹음해서 Azure 종합점수가 기대한 대로 나오는지, 문장이 좁은 카드에서 잘 보이는지 다음 세션에서 확인.

## 학생 관리 기능 (구현 완료, Supabase 서버 연결까지 됨)

사용자가 "한글또박또박" 같은 기존 검사 프로그램(스크린샷)을 참고 사례로 보여주며 요청. 원래는 "1~6단계 먼저 마무리 후 착수"로 정했었는데, 사용자가 자리를 비우며 "지금 바로 만들어달라"고 요청해서 2026-08-31에 구현함.

- **로그인**: 이름 + 비밀번호. 처음 쓰는 이름이면 그 자리에서 새 계정으로 만들어짐(가입 절차 따로 없음). `handleLogin`.
- **반 관리**: 로그인 후 반 목록 화면(`renderClassList`), "+ 반 추가"로 반 생성.
- **학생 목록**: 반을 고르면 학생 목록 화면(`renderStudentList`) — 번호/이름/평균 점수 표, "검사 시작"(그 학생으로 1~6단계 연습 진입), "결과 보기"(그 학생의 세션 기록), 삭제.
- **학생 수 추가**: 원하는 인원수만큼 빈 학생 행을 한 번에 추가(번호 자동 증가, 이름은 표에서 바로 입력).
- **엑셀 업로드/양식**: SheetJS(xlsx, CDN)로 구현. 양식은 **1열=번호, 2열=이름**, 첫 줄은 헤더로 취급해 건너뜀. "엑셀 양식 다운로드" 버튼으로 견본 파일을 내려받을 수 있음(`downloadStudentTemplate`). 업로드는 `handleExcelUpload`에서 파싱.
- **버그 수정**: "반 추가" 버튼이 반응 없다는 피드백 → 원인은 `window.prompt()` 팝업이 이 환경에서 조용히 안 뜨는 것으로 추정됨. `prompt()`/`confirm()`/`alert()` 같은 브라우저 네이티브 팝업을 전부 화면 안 입력창/문구로 바꿈 (반 추가는 입력창+버튼, 학생 삭제는 "한 번 더 누르면 삭제" 2단계 클릭, 로그인/엑셀 오류는 화면에 빨간 문구로 표시).

### Supabase 서버 연결 (완료, 2026-08-31)

- **DB**: Supabase 선택함 (Vercel Postgres보다, 로그인 기능이 내장돼 있고 대시보드에서 데이터를 직접 볼 수 있어서). 사용자가 Supabase 프로젝트 생성 후 Project URL / publishable key / secret key 전달함.
  - Project URL, publishable/secret key는 `.env.local`에 저장 (git 제외).
  - publishable key는 브라우저에 그대로 노출해도 안전하도록 설계된 키라서 `index.html`에 상수로 직접 넣음. secret key는 지금은 어디에도 안 쓰고 있음(나중에 서버 전용 관리 기능이 필요해지면 사용).
- **DB 구조**: [supabase/schema.sql](../supabase/schema.sql) 파일로 정의함 — `classes`(반), `students`(학생), `sessions`(학생별 단계 검사 기록, `items`는 jsonb) 3개 표. Row Level Security로 "선생님은 본인이 만든 데이터만" 접근 가능하게 제한함. **사용자가 Supabase 대시보드 SQL Editor에서 직접 실행해야 적용됨** — 실행 여부 다음 세션에서 확인 필요.
- **로그인 방식**: Supabase Auth(이메일+비밀번호) 사용. 다만 실제 이메일 없이 "이름+비밀번호"만으로 쓰게 하려고, 이름을 결정적 해시로 변환해 가짜 이메일(`t{해시}@hangeul.local`)을 만들어 내부적으로만 씀 (`emailFor` 함수). 처음 쓰는 이름이면 그 자리에서 자동 가입(`signUp`), 있으면 로그인(`signInWithPassword`).
  - **사용자가 Supabase 대시보드에서 "Confirm email"을 꺼야만** 가입 즉시 로그인이 됨(안 끄면 이메일 인증 대기 상태가 되어 로그인 실패) — 다음 세션에서 이것도 확인 필요.
- **CDN 로드**: `supabase-js@2`를 `https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2`로 불러옴 (설치 없이 사용).
- 로그인 후 `loadClasses()`로 반+학생+검사기록을 한 번에 불러와 `state.classes`에 캐시하고, 이후 추가/수정/삭제할 때마다 Supabase에 즉시 반영(`insert`/`update`/`delete`) + 로컬 캐시도 같이 갱신. 검사 점수는 `saveSessionItems()`로 매 문항마다 저장.

### 구글 로그인 연동 (2026-09-02, 코드는 완료·Google/Supabase 대시보드 설정은 사용자 확인 필요)

- 사용자가 "카카오/구글 로그인도 붙이고 싶다"고 요청 → 설정이 더 간단한 **구글부터 먼저** 붙이기로 함(카카오는 이메일 등 프로필을 더 받으려면 비즈니스 인증이 필요할 수 있어 나중에 검토).
- 기존 이름+비밀번호 로그인은 그대로 두고, 로그인 화면에 "🔵 구글 계정으로 로그인" 버튼을 추가하는 방식(대체가 아니라 추가)으로 구현함.
- **중요**: 이름+비밀번호 계정은 가짜 이메일(`t해시@hangeul.local`)을, 구글 로그인은 진짜 gmail 주소를 쓰기 때문에 Supabase 입장에서 서로 완전히 다른 계정이다. 즉 이름+비밀번호로 이미 만들어둔 반/학생 데이터는 구글로 로그인하면 보이지 않는다(반대도 마찬가지). 발표 때 어느 쪽으로 로그인할지 미리 정해두는 게 좋음.
- 코드 변경: `renderLogin`에 구글 버튼 추가, `handleGoogleLogin`(`sb.auth.signInWithOAuth({provider:'google'})`으로 구글 로그인 화면으로 이동), `enterAsTeacher`(로그인 성공 후 공통 처리를 함수로 뽑음, 이름+비밀번호/구글/자동 재로그인 세 곳에서 재사용), 파일 맨 아래 페이지가 처음 열릴 때 `sb.auth.getSession()`으로 이미 로그인된 세션이 있는지 확인하는 로직 추가 (구글 로그인 후 돌아왔을 때, 그리고 이전에 로그인한 채로 브라우저를 나중에 다시 열었을 때 둘 다 로그인 화면을 건너뛰고 바로 반 목록으로 들어가게 함).
- **사용자가 직접 해야 하는 나머지 절차** (계정 소유자만 가능해서 에이전트가 대신 못 함):
  1. [Google Cloud Console](https://console.cloud.google.com/)에서 새 프로젝트 생성 → "API 및 서비스 > OAuth 동의 화면"에서 외부(External) 유형으로 앱 정보 입력 (테스트 상태로 두면 별도 심사 없이 "테스트 사용자"로 등록한 계정만 로그인 가능 - 본인/동료 교사 이메일을 테스트 사용자에 추가).
  2. "사용자 인증 정보 > OAuth 클라이언트 ID" 만들기 (유형: 웹 애플리케이션), **승인된 리디렉션 URI**에 `https://idvfmahxmdgyjncgtjxa.supabase.co/auth/v1/callback` 추가 → Client ID / Client Secret 발급.
  3. Supabase 대시보드 > Authentication > Providers > Google 활성화 → 위 Client ID/Secret 입력 → 저장 (이 키들은 Supabase 쪽에만 등록하면 되고, 우리 프로젝트의 `.env.local`이나 Vercel 환경 변수에는 넣지 않는다 - Supabase가 구글과의 통신을 대신 처리해주기 때문).
  4. Supabase 대시보드 > Authentication > URL Configuration > Redirect URLs에 `http://localhost:3000` 추가 (로컬 테스트용, 나중에 Vercel 배포 주소도 추가해야 함).
- 사용자가 Google Cloud Console에서 OAuth 클라이언트 생성 완료, Supabase Provider에 Client ID/Secret 등록 완료. Redirect URLs에 `http://localhost:3000` 추가까지 안내함. **브라우저로 실제 "구글로 로그인" 버튼 클릭 테스트는 아직 결과를 못 받음** - 다음 세션에서 제일 먼저 확인.

### 결과 화면 재설계 + 검사지 리포트 + 학생 특성 체크박스 (2026-09-02)

- 사용자가 결과 확인 화면이 "시도한 순서대로"라 헷갈리고 전문적이지 않다고 피드백 → `renderHistory`를 **커리큘럼 단계(1.모음~6.대표받침) 순서로 묶어서** 보여주도록 재설계함. 각 단계는 카드 형태로 "최근 점수 + 도달/보완 필요 배지 + 시도 횟수"를 보여주고, 한 단계를 여러 번 시도했으면 탭했을 때 시도 목록(`renderStageAttempts`, 신규 화면)을 먼저 보여준 뒤 문항별 상세(`renderSessionDetail`)로 들어가게 함 (한 번만 시도했으면 목록 없이 바로 상세로).
- 참고용으로 보여준 "한글또박또박" 검사지 스크린샷을 본떠서 **인쇄 가능한 검사지 리포트**(`renderReport`, 신규 화면)를 추가함. 학생 목록의 "리포트" 버튼으로 진입. 학생 정보(반/번호/이름/다문화/기초학습지원대상) 표 → 종합결과(검사일시/전체 평균/수준) 표 → 자동 생성 종합의견(보충 필요 항목 나열, 없으면 칭찬 문구) → 항목별 세부결과 표(단계명/맞은 문항 수 대비 전체/도달여부/오반응 목록) 순서로 구성. 별도 PDF 라이브러리 없이 `@media print` CSS + `window.print()`만으로 인쇄/PDF 저장을 지원함 (결과물 형식 우선순위 원칙에 맞게 외부 라이브러리 추가를 피함).
- 학생별 **"다문화"/"기초학습지원대상" 체크박스**를 학생 목록 표에 추가함(사용자 요청). Supabase `students` 테이블에 `multicultural`, `basic_support` 불리언 컬럼 추가 필요 — **`supabase/schema.sql` 맨 아래 "마이그레이션" 섹션의 `alter table` 두 줄을 SQL Editor에서 한 번 더 실행해야 실제로 작동한다** (기존 `create table if not exists`는 이미 있는 테이블에는 적용 안 됨). 체크박스를 누르면 바로 Supabase에 저장됨.
  - 참고: 이 두 항목은 "이름+점수" 수준을 넘어서는 학생 개인정보라서, [00번 가이드](00-프로젝트-에이전트-가이드.md)의 "저장 항목 최소화" 원칙에서 벗어나는 확장이다. 다만 접근 제어(RLS)는 테이블 전체에 이미 적용돼 있어 별도 보안 설계 없이도 "선생님 본인 반만" 원칙은 그대로 유지됨. 사용자가 프로토타입 단계에서 개인정보 정식 검토는 생략하기로 명시적으로 결정함(2026-09-02).
- 문법 검사 + node로 리포트 집계 로직(단계별 최근 시도 찾기, 전체 평균, 실패 단계 목록)만 따로 시뮬레이션해서 확인함. **브라우저로 실제 화면 확인은 아직 못 함** - 다음 세션에서 확인 필요.

### 사용자 실사용 피드백 반영 (2026-09-02, 위 기능들 실제로 써본 뒤)

1. **다문화/기초학습지원대상 저장 안 됨** — "Could not find the 'basic_support' column" 오류 발생. 원인은 위 마이그레이션 SQL을 아직 Supabase에서 실행하지 않아서. 코드 문제 아님 — 다음 세션 시작할 때 실행 여부부터 확인.
2. **세션 상세 화면에 "무엇으로 오발음했는지" 안 보임** — 그동안 `session.items`에 인식된 소리(recognizedText)를 아예 저장하지 않고 있었음(맞았는지/점수/이유만 저장). `assessRecording`에서 `recognizedText`도 함께 저장하도록 고치고, `renderSessionDetail` 표에 "인식된 소리" 열을 추가함.
3. **세션 상세 표에 헤더(열 이름)가 없었음** — "문항/점수/인식된 소리/오류 내용" 헤더 행을 추가함.
4. **결과 화면들에서 학생 목록으로 바로 못 감** — `renderHistory`와 `renderSessionDetail`에 기존 버튼("단계 목록으로"/"결과 목록으로")은 그대로 두고 **"학생 목록으로" 버튼을 나란히 추가**함 (어디서 들어왔든 한 번에 학생 목록으로 돌아갈 수 있게).
5. **검사지 리포트에 "4. 추천 학생 지도 방안" 추가** — 실시간 AI 호출(Claude API) vs 규칙 기반 미리 작성한 문구 중 사용자가 **규칙 기반**을 선택함(유료 API/서버 불필요, 인터넷 없이도 동작, 오늘 안에 완성 가능해서). `STAGE_ADVICE`에 07/08번 문서 내용을 바탕으로 단계별 지도 문구를 미리 써두고, `buildAdviceList`가 "보완 필요"로 나온 단계의 문구만 골라 보여줌. 실시간 AI 호출 옵션은 안 씀 — 나중에 필요하면 재검토.

### 위 5가지를 실제로 써본 뒤 다시 받은 피드백 (2026-09-02, 같은 날 두 번째 라운드)

- **2번 관련**: "인식된 소리"가 계속 "-"로만 나온다는 피드백 → 확인해보니 실제로는 정상 동작이었고, 사용자가 보고 있던 세션이 `recognizedText`를 저장하기 **이전**(이날 세션 초반)에 만들어진 오래된 기록이라 그 필드 자체가 없었던 것뿐이었음(코드 버그 아님). 다만 화면에 "-"만 뜨면 마치 고장난 것처럼 보이므로, `renderSessionDetail`에서 "예전 기록이라 없음"과 "인식은 했지만 소리를 못 얻음"을 구분해서 보여주도록 문구를 다듬음. **새로 시작하는 검사부터는 정상적으로 인식된 소리가 보여야 함** — 다음 세션에서 새 세션으로 재확인 필요.
- **리포트의 "오반응" 표시 방식 개선**: 기존엔 오답 글자만 나열했는데(예: "버스, 수바"), "문항이 뭐였고 어떻게 오반응했는지"가 더 중요하다는 피드백 → `formatWrongItem` 함수를 추가해서 "문항→인식된 소리"(예: "버스→거스") 형태로 표시하도록 "3. 항목별 세부 결과" 표를 바꿈. "모르겠어요"로 넘긴 문항은 "문항(모르겠음)"으로 구분 표시.
- **"4. 추천 학생 지도 방안" 문구 개선**: "특히 '워'에서 오류가 보였어요"처럼 단어 하나만 콕 집는 문구는 필요 없고, **무엇이 부족한지(받침 발음, 받침 탈락 등)를 유형별로 상세히 설명**해달라는 요청 → `summarizeErrorTypes` 함수를 추가해서, 그 단계의 오답들을 채점 피드백 문구 기준으로 "받침 탈락/교체" / "초성·중성 자체가 다른 더 기초적인 오류" / "발음은 맞았지만 소리 크기·유창성 감점" / "아예 모르겠다고 답함" 네 유형으로 나눠서, 각 유형이 있을 때만 그 유형 설명 + 해당 문항 목록을 문장으로 붙여 보여주도록 바꿈.
- 위 항목들 모두 문법 검사 + node 시뮬레이션으로 로직만 확인함. 브라우저 실사용 재확인 필요.

### 세 번째 라운드 피드백 (2026-09-02, 실제 검사 중 "따" 발음 사례)

- **"따"를 정확히 발음했는데 58점(불합격)이 나와서 이상하다는 피드백** → 확인해보니 버그가 아니라 정상 동작: "인식된 소리: 따"로 목표와 정확히 일치했지만, Azure의 세부 점수 중 유창성(FluencyScore)이 낮게 나와서(`explainLowScore`가 "말이 자연스럽게 이어지지 않았어요" 이유를 보여줌) 그대로 낮은 점수를 준 것. 정확한 단어를 말해도 발음 품질이 떨어지면 낮은 점수를 주는 게 Azure 발음 평가를 쓴 원래 목적([10번 문서](10-음성인식-기술-선택.md))이라 의도된 동작이지만, 짧은 한 음절만 녹음하면 "유창성" 지표 자체가 원래 여러 음절/문장에 최적화된 지표라 불안정할 수 있다는 가능성은 남아있음 - 나중에 이런 사례가 자주 보이면 단음절 단계(1~2단계)에 한해 FluencyScore 비중을 낮추는 걸 검토하기로 함 (아직 코드는 안 건드림).
- 리포트의 "오답 문항"/"인식된 소리"가 같은 글자로 나란히 보이는 게(예: "따" | "따") 이 상황을 오히려 명확하게 보여준다는 걸 확인함(아래 표 개편 덕분).
- **리포트 표 가독성 개선**: "오반응" 한 칸에 "빠→봐, 까→가, 따→따"처럼 여러 개를 몰아넣던 걸, **"오답 문항"과 "인식된 소리" 두 열로 분리**(줄바꿈으로 문항별로 나란히 대응)해서 한눈에 보기 쉽게 바꿈.
- **"4. 추천 학생 지도 방안" 구조 개편**: 한 문단에 다 이어붙이던 걸 **"1. 2. 자음" 같은 큰 항목 + "가/나/다..." 하위 항목**(기본 지도 원리 한 줄 + 오류 유형별 설명 한 줄씩) 구조로 바꿈 (`renderAdviceSection` 신규, 기존 `buildAdviceList`/`formatWrongItem` 대체). 하위 항목의 예시 글자도 화살표 없이 문항 이름만 나열하도록 단순화함(어떤 소리로 들렸는지는 이제 표에서 바로 보이므로 중복 설명 제거).
- 문법 검사 + node 시뮬레이션으로 확인함. 브라우저 실사용 재확인 필요.

### 오류 유형 진단 태그 추가 (2026-09-02, 네 번째 라운드)

- 사용자가 세션 상세 화면의 "오류 내용"도 07/08번 문서 용어(받침 탈락 등)로 더 전문적으로 표기해달라고 요청.
- **`analyzeError(it)` 함수 신설** — 오답 문항 하나의 목표 글자와 인식된 소리를 초/중/종성 단위로 직접 비교해서, "받침 탈락"/"받침 첨가"/"받침 교체"/"초성(자음) 오류"/"중성(모음) 오류"/"음절 수 오류"/"유창성·음량 문제"/"모름"/"인식 실패" 중 해당하는 태그를 (한 문항에 여러 개 겹칠 수 있어서) 배열로 반환함. `classifyMismatch` 바로 아래에 정의해서 세션 상세 화면과 리포트 지도 방안이 똑같은 기준을 공유하도록 함(기존엔 각자 다른 방식으로 오류를 나누고 있었음).
  - 예: '까'→'가'는 ["초성(자음) 오류"], '막'→'마'는 ["받침 탈락"], '강'→'감'은 ["받침 교체"], '라'→'락'은 ["받침 첨가"], 정확히 인식됐지만 저품질인 경우(예: '따'→'따')는 ["유창성/음량 문제"].
- `renderSessionDetail`의 "오류 내용" 칸에 이 태그를 작은 주황색 배지(`.error-tag`)로 보여주고, 그 아래 기존 안내 문구(it.feedback)를 작게 함께 보여주도록 바꿈.
- 리포트의 `summarizeErrorTypes`도 자체적인 feedback 문자열 매칭 대신 `analyzeError`+`ERROR_TAG_DESC`를 재사용하도록 리팩터링함 (한 곳만 고치면 두 화면 모두 반영됨).
- node로 '빠'→'봐'(초성+중성 오류), '까'→'가'(초성 오류), '따'→'따'(유창성 문제), '막'→'마'(받침 탈락), '강'→'감'(받침 교체), '라'→'락'(받침 첨가) 등 사례를 시뮬레이션해서 전부 의도대로 분류되는 걸 확인함. 브라우저 실사용 재확인 필요.

### 다섯 번째 라운드 피드백 - 가독성 + 문구 통일 + 지도법 추가 (2026-09-02)

1. **세션 상세 화면 가독성 개선**: 오류 태그 아래에 "목표 글자와 다른 소리예요. 다시 발음해보세요" 같은, 태그만으로 이미 다 설명되는 뻔한 문구가 매번 반복돼서 지저분하다는 피드백 → `GENERIC_FEEDBACK_TEXTS` 집합을 만들어 이 문구들(받침이 정확하지 않아요/목표 글자와 다른 소리예요.../모르겠다고 답함)은 태그 아래에 다시 보여주지 않고, 태그만으로 설명 안 되는 진짜 세부 사유(SNR·유창성 등 explainLowScore 문구)일 때만 보여주도록 바꿈. 또한 이 화면이 좁은 카드(480px)에 4열짜리 표를 욱여넣어 글자가 한 자씩 줄바꿈되던 문제 → `sessionDetail`을 `WIDE_SCREENS`에 추가하고 표 스타일도 기존 `.data-table`과 같은 계열인 `.report-table`로 통일함.
2. **리포트 표 가독성 개선(2차)**: "오답 문항"/"인식된 소리"를 한 칸에 `<br>`로 여러 줄 쌓아서 줄 수로 대응시키던 방식이 여전히 헷갈린다는 피드백 → 오답 문항 하나당 표의 행(`<tr>`)을 아예 하나씩 따로 만들고, "항목/점수/도달여부" 3칸은 `rowspan`으로 묶어 한 번만 보여주는 구조로 바꿈(실제 진단 보고서에서 흔히 쓰는 표 구조). 단계가 바뀌는 첫 행 위에는 옅은 굵은 선을 그어 단계끼리 시각적으로 구분함.
3. **지도 방안 문구 통일 + 유형별 지도법 추가**: "받침보다 더 기초적인 오류 (빠, 까)."처럼 묘사만 하고 끝나는 문장과, "~연습시켜보세요"로 끝나는 실행 지침이 섞여 있어 통일감이 없다는 피드백 + 오류 유형마다 구체적으로 어떻게 지도하면 좋을지 추천해달라는 요청 → `ERROR_TAG_DESC`(설명만 있던 사전)를 **`ERROR_TAG_ADVICE`(문항 목록을 받아 "무엇이 문제였는지 + 어떻게 지도하면 좋을지"를 한 문장으로 만드는 함수 사전)로 교체**하고, `STAGE_ADVICE`의 6개 문장도 전부 "~연습시켜보세요"로 끝나게 다듬음. 유형별 지도법:
   - **받침 탈락**(예: 막→마): 받침 소리를 과장해서 들려주고 끝까지 붙여 말하는 연습
   - **받침 첨가**(예: 라→락): 받침 없는 글자와 있는 글자를 나란히 비교하는 연습
   - **받침 교체**(예: 강→감): 같은 자리에서 받침만 다른 낱말끼리(강/감/간) 비교하는 연습
   - **초성(자음) 오류**: 조음 위치가 비슷한 자음끼리(ㅁ↔ㅂ, ㅇ↔ㄱ) 입 모양을 비교하는 연습
   - **중성(모음) 오류**: 천지인 원리로 입 모양·밝은소리/어두운소리 차이를 비교하는 연습
   - **음절 수 오류**: 글자를 하나씩 손으로 짚어가며 음절 수만큼 끊어 읽는 연습
   - **유창성/음량 문제**(예: 정확히 인식된 '따'가 저품질): 목소리를 크고 끊기지 않게 이어 말하는 연습
   - **모름/인식 실패**: 다시 들려주고 따라 말하기부터, 또는 마이크에 더 가까이 말하도록 안내
- 문법 검사 + node 시뮬레이션(표 rowspan 구조, 문구 생성)으로 확인함. 브라우저 실사용 재확인 필요.

### 학급 통계 화면 추가 + 전반적 가독성 개선 (2026-09-02, 여섯 번째 라운드)

- 사용자가 다른 검사 프로그램("한글또박또박")의 학급통계 화면 스크린샷을 보여주며 비슷한 기능을 요청함. 그 화면엔 **1차/2차 검사 회차 구분·12개 세부항목·유창성/쓰기 채점**처럼 지금 우리 앱에 없는 개념이 많아서, 사용자와 상의해서 **지금 있는 6단계 구조 그대로 반 전체를 집계**하는 방향으로 범위를 단순화하기로 함(전체 재현은 나중에 필요하면 재검토). 그래프 구현은 **Chart.js를 CDN으로 불러와** 사용하기로 함(직접 SVG로 그리는 대신 - 사용자가 선택).
- **`renderClassStats`(신규 화면)**: 학생 목록 화면에 "📊 학급통계" 버튼을 추가해서 진입. `computeClassStats(cls)`가 반 전체 데이터를 집계함:
  - 요약 카드: 총 인원, 검사 실시 인원·진행률, 최근 검사일, 미실시 학생 번호
  - 도넛차트 "검사 응시율"(응시/미응시), 파이차트 "한글 해득수준 비율"(해득 70+/보충 50~69/미해득 <50/미검사, 우리 앱의 기존 70점 합격 기준과 통일)
  - 막대그래프 "세부항목별 평균점수" 3개: **전체 학급 / 다문화 학생집단 / 기초학습지원대상 학생집단**(원본 화면의 해득/보충/미해득 그룹 대신, 사용자가 요청한 다문화·기초학습지원대상 체크박스로 그룹을 나눔) - 각 집단 차트 아래에 해당 학생 번호 목록도 표시. 체크된 학생이 없는 집단은 차트 대신 안내 문구만 표시.
  - node로 학생 3명(다문화 1명, 기초학습지원 2명, 미실시 1명 포함) 가상 데이터를 만들어 집계 결과(해득수준 분류, 단계별 집단 평균)가 손계산과 일치하는지 확인함.
- **전반적 가독성 개선**: `.data-table`/`.report-table`에 표 행 줄무늬(zebra striping, 짝수 행 옅은 회색)와 마우스 올렸을 때 강조(hover) 효과를 추가하고, 화면마다 제각각이던 "N. 제목" 스타일 문구들을 `.section-title` 공통 클래스로 통일함(리포트의 1~4번 제목, 학급통계의 각 박스 제목, 결과 확인 화면의 "🔍 검사 결과" 등).
- Chart.js CDN(jsdelivr, 버전 4.4.4 고정)이 실제로 응답하는지 curl로 확인함. 문법 검사 + node 시뮬레이션으로 로직 확인함. **브라우저에서 실제 차트가 정상적으로 그려지는지는 아직 확인 못 함** - 다음 세션에서 최우선으로 확인 필요 (Chart.js를 실제로 처음 붙여보는 것이라 리스크가 있음).

### 학급통계 피드백 반영 (2026-09-02, 일곱 번째 라운드)

1. **표 hover 강조가 zebra striping과 겹쳐 안 보임** — 짝수 행 배경(#f9fafb)과 hover 배경(#f3f4f6)이 색이 너무 비슷해서 구분이 안 됐음 → hover 배경을 아예 다른 색 계열(연한 파랑 #eff6ff, 앱 다른 곳의 버튼 hover와 같은 톤)로 바꿔서 항상 뚜렷하게 보이게 함.
2. **"오류 유형별 학생" 섹션 추가**: 학급통계 화면 맨 아래에 "받침 탈락: 1. 김상혁"처럼 오류 유형마다 해당하는 학생을 나열하는 섹션을 추가함. `computeClassStats`에 `errorTypeStudents` 집계를 추가했는데, 학생별로 6단계 각각의 가장 최근 시도만 보고(이미 고친 옛날 오류는 제외), 그 안의 오답 문항들을 `analyzeError`로 분류해서, 같은 학생이 같은 유형을 여러 번 냈어도 한 번만 나열되게 함. `renderErrorTypeStudents` 함수가 `ERROR_TAG_ADVICE`에 정의된 순서 그대로 화면에 표시함(순서가 매번 바뀌지 않게).
- node로 학생 2명(각각 받침 탈락 1건, 초성 오류 1건) 가상 데이터를 만들어 집계 결과를 확인함. 브라우저 실사용 재확인 필요.

### 여덟 번째 라운드 피드백 (2026-09-02)

1. **"모름" → "읽지 못함"으로 용어 교체**: 더 전문적인 용어로 바꿔달라는 요청 → `analyzeError`/`ERROR_TAG_ADVICE`의 '모름' 태그를 '읽지 못함'으로 통일함 (읽기 진단 앱 맥락에 더 맞는 표현).
2. **"오류 유형별 학생" 목록에서 "(예전 기록)" 제외**: 실제 오류 유형이 아니라 recognizedText 저장 이전의 옛날 데이터라는 표시일 뿐이라, `computeClassStats`의 errorTypeStudents 집계 시 이 태그만 걸러내도록 함(세션 상세/리포트 등 다른 화면에는 그대로 남아있음 - 거긴 "왜 정보가 없는지" 설명하는 용도라 유지).
3. **표 줄무늬(zebra striping) 제거**: hover 강조색을 바꾼 뒤에도 다시 보니 굳이 필요 없다고 판단 → 짝수 행 배경 규칙을 삭제하고 hover 강조만 남김.
- 문법 검사 + node 시뮬레이션으로 확인함. 브라우저 실사용 재확인 필요.

### 아홉 번째 라운드 - Vercel 배포 + UI 정리 (2026-09-02 밤 ~ 2026-09-03)

- **Vercel 배포 완료**: `tmdry1999-lgtm/hangeul` 저장소를 Vercel에 import, 환경 변수 `AZURE_SPEECH_KEY`/`AZURE_SPEECH_REGION` 등록 후 배포 성공. 실제 주소: **https://hangeul-phi.vercel.app/**. Vercel 서버리스 함수가 ESM(`export default`) 문법을 쓰므로 `package.json`에 `"type": "module"`을 추가해서 명시적으로 선언함.
- **구글 로그인이 localhost로 튕기는 문제**: 코드는 이미 `window.location.origin`(접속한 주소 그대로)을 리다이렉트 주소로 쓰고 있어서 코드 수정은 불필요했고, 원인은 Supabase Authentication > URL Configuration에 아직 `http://localhost:3000`만 등록돼 있던 것. Site URL을 배포 주소로 바꾸고 Redirect URLs에 `https://hangeul-phi.vercel.app/**`를 추가해서 해결함.
- **표/버튼 줄바꿈 정리**: 학생 목록 표에서 "다문화"/"기초학습지원대상"이 어절 중간에서 끊기고, "검사 시작"/"결과 보기" 버튼도 아무 데서나 줄바꿈되던 문제 → `.data-table`/`.pill-btn`에 `word-break: keep-all`을 추가해서 띄어쓰기(어절 경계)에서만 줄이 바뀌게 함.
- **로고 클릭 시 반 목록으로 이동**: 상단 "한글또박 발음 지도" 제목을 로그인 상태에서 누르면 로그인 직후 화면(반 목록, `classList`)으로 바로 이동하도록 클릭 이벤트 추가.
- **한글 해득수준 기준 구체화 (사용자 선택: 6단계 완주 기준)**: 기존엔 "지금까지 푼 모든 문항의 평균 점수"만 보고 70/50점 경계로 해득/보충/미해득을 나눠서, 1단계(모음)만 풀고 100점 받은 학생과 6단계를 전부 통과한 학생이 똑같이 "해득"으로 묶이는 문제가 있었음. 07/08번 문서(찬찬한글의 단계적 학습 위계 + "최종평가" 개념)를 근거로 기준을 다시 정의함:
  - **해득**: 6단계(모음~대표받침) 전부 시도 + 전부 70점 이상 도달
  - **미해득**: 모음·자음처럼 가장 기초 단계부터 70점 미만
  - **보충**: 그 외(진행 중이거나 중간 단계에서 70점 미만이 있음)
  - **미검사**: 세션 기록 없음
  - 이 계산(`computeStudentStats`)과 등급 판정(`achievementTier`)을 학급통계와 학생 목록 화면이 공유하도록 함수로 분리함. 학급통계 파이차트 아래에 이 기준을 설명하는 문구(미검사 제외)를 추가하고, 학생 목록 표의 이름 아래에도 해득/보충/미해득/미검사 배지를 추가함(해득=초록, 보충=주황, 미해득=빨강, 미검사=회색 - 사용자가 미해득/미검사 색을 직접 지정함).
  - **오류 유형별 학생 섹션 표로 재구성**: 처음엔 오류 유형·학생 이름 모두 노란 알약(`.error-tag`) 모양으로 만들었다가, "알약 말고 다른 표들처럼 깔끔하게"라는 피드백을 받고 일반 표(테두리 있는 셀 + 쉼표로 구분된 이름 텍스트)로 다시 바꿈. 학생 번호도 빼고 이름만 표시하도록 함께 정리.
- **검사 결과 화면에 리포트 버튼 추가**: "단계 목록으로"/"학생 목록으로" 옆에 같은 크기(`secondary-btn`)의 "리포트" 버튼을 추가해서, 결과 목록 화면에서도 바로 인쇄용 리포트로 이동할 수 있게 함(`openReportForCurrentStudent` 함수로 학생 목록의 리포트 버튼과 로직 공유).
- **지도방안 번호 중복 제거**: "1. 6. 대표받침"처럼 항목 번호와 단계 이름의 번호가 겹쳐 보이던 것 → 앞의 항목 번호를 빼고 단계 이름(이미 번호 포함)만 보이도록 정리.
- 모두 로컬 브라우저에서 사용자가 직접 확인 후 승인함.

### 열 번째 라운드 - 재생 버튼 + 문장/문단 단어별 오류 진단 (2026-09-03)

사용자가 문장/문단 단계를 실제로 검사해본 뒤(위 캡처 - "나비가 꽃밭에서~"가 32점, "강아지와 산책을~"이 40점) 두 가지를 요청함.

1. **세션 상세 화면에 재생 버튼 추가**: "인식된 소리 밑에 재생 버튼을 눌러서 다시 녹음한 걸 들을 수 있냐"는 요청. `stopRecording`에서 만드는 WAV 녹음 파일(`wavBlob`)을 `assessRecording`에서 `URL.createObjectURL`로 메모리에 담아 `state.recordingUrls`(key: `세션id_문항번호`)에 저장하고, `renderSessionDetail`의 "인식된 소리" 칸 아래에 "▶ 재생" 버튼을 추가함(있을 때만). **서버(Supabase)에는 저장하지 않음** — [00번 가이드](00-프로젝트-에이전트-가이드.md)의 "학생 개인정보(음성 포함)는 로컬에만" 원칙과 같은 방향으로, 이 브라우저 탭이 열려있는 동안만 재생 가능하고 새로고침하거나 다른 컴퓨터에서 보면 재생 버튼 자체가 안 뜬다(방금 그 자리에서 녹음한 문항만 가능) - 이 트레이드오프를 사용자에게 설명하고 진행함.
2. **문장/문단 점수 체계 재검토**: 32점·40점처럼 낮은 문항들을 실제로 보니 "발음이 나빠서"가 아니라 인식 자체가 문장 중간 단어를 통째로 놓친 경우였는데, 기존엔 원인 구분 없이 "유창성/발음 문제" 태그 하나로만 뭉뚱그려 보여주고 있었음. 에이전트가 3가지 안(① 완성도 안전장치만 추가 ② 단어별 오류 진단 추가 ③ 둘 다)을 제시했고 **사용자가 "단어별 오류 진단 추가"를 선택함** (점수 자체는 PronScore를 그대로 헤드라인으로 유지, 대신 왜 그 점수가 나왔는지의 진단을 정교화하는 방향).
   - Azure REST 응답의 `NBest[0].Words` 배열(문장 안 단어마다 `ErrorType`: Omission/Insertion/Mispronunciation/None)을 새로 읽어서 씀. **참고**: Azure 공식 문서 예시는 단어별 점수도 `Words[i].PronunciationAssessment.{AccuracyScore,ErrorType}`처럼 한 번 더 감싸진 형태로 나오는데, 이 프로젝트가 쓰는 REST API 버전은 문장 전체 점수조차 과거에 문서와 다르게(감싸지 않은 형태로) 왔던 전례가 있어서(위 "완료된 작업" 1차 시도 참고), `wordAssessmentField()` 헬퍼로 감싸져 있든 아니든 둘 다 읽을 수 있게 방어적으로 짬 — 브라우저 실사용 테스트에서 실제로 어느 형태로 오는지 콘솔 로그로 확인 필요.
   - `analyzeError`의 문장/문단 분기(띄어쓰기 있는 문항)를 "무조건 '유창성/발음 문제' 하나"에서, `wordIssues`가 있으면 그 안의 `ErrorType`을 보고 **`단어 생략`/`불필요한 말 추가`/`단어 발음 오류`** 새 태그로 세분화하도록 바꿈 (`wordIssues`가 없는 예전 기록이거나 Azure가 단어 정보를 못 준 경우엔 기존처럼 뭉뚱그린 태그로 안전하게 대체). `ERROR_TAG_ADVICE`에도 이 3개 태그의 지도 문구를 추가했는데, `wordListForType()` 헬퍼로 "어떤 단어가" 문제였는지까지 문구에 구체적으로 넣음.
   - 세션 상세 화면(`renderSessionDetail`)의 "오류 내용" 칸에도 태그 배지 아래에 `'꽃밭에서'(생략), '나비가'(발음 오류)`처럼 단어별 상세를 한 줄 추가로 보여줌.
   - `ERROR_TAG_ADVICE`가 이미 세션 상세·리포트 지도 방안·학급통계 "오류 유형별 학생" 세 화면에서 공통으로 쓰이는 구조라서, 새 태그 3개를 사전에 추가하는 것만으로 세 화면 모두에 자동 반영됨(추가 코드 수정 불필요).
   - node로 (2번 문항처럼) 단어 생략+발음오류가 섞인 사례와 (3번 문항처럼) 단어 정보가 없는 예전 기록 사례를 시뮬레이션해서 태그 분류와 지도 문구가 의도대로 나오는지 확인함.
   - 전면 수정 전 습관에 따라 이번 수정 직전 상태를 `index-backup-2026-09-03.html`로 백업해둠(git에는 올리지 않음, 로컬 참고용).
   - **브라우저 실사용 재확인 필요** — 특히 Azure가 실제로 `Words` 배열과 각 단어의 `ErrorType`을 기대한 형태로 주는지(위 "참고" 항목), 재생 버튼이 실제로 소리를 들려주는지 다음 세션(또는 이어서)에서 최우선 확인.

### 열한 번째 라운드 - 문장 채점 기준 재조정 (2026-09-03, 위 기능 실사용 직후)

위 재생 버튼 + 단어별 오류 진단을 실제로 써보고 두 가지 문제를 더 발견함.

1. **단어가 명백히 틀렸는데도 합격 처리되고 오류 내용이 안 보임**: "강아지가 밥을 먹어요"를 "감마쥐가 밥을 먹어요"로 잘못 인식했는데 76점(합격)으로 처리됨. "아기가 잠을 자요"도 일부러 받침을 다 빼고 "자을 자요"로 읽었는데 86점(합격)이 나옴. 원인은 두 가지가 겹친 것: ① `errorCell`이 `it.pass`가 false일 때만(=70점 미만일 때만) 오류 내용을 보여주는데, PronScore가 단어 하나 정도 틀려도 70점을 잘 안 내려가서 합격으로 묻힘. ② 그러다 보니 열 번째 라운드에서 추가한 단어별 오류 진단(`wordIssues`)도 애초에 화면에 뜰 기회가 없었음.
   - **해결**: `assessRecording`의 `compositeScoring` 분기에서, `wordIssues`가 하나라도 있으면(생략/삽입/발음오류 중 어떤 것이든) PronScore가 아무리 높아도 점수를 합격선(70점) 아래인 **69점으로 낮추도록** 바꿈(`accuracy = Math.min(accuracy, 69)`). 이러면 자동으로 불합격 처리되어 오류 내용도 함께 보이게 된다. (사용자는 "80점 아래로" 낮추자고 제안했는데, 기존 합격 기준이 70점이라 그보다 낮은 69점으로 확실히 불합격 처리되게 함 - 80점만 낮추면 여전히 합격으로 처리돼 오류 내용이 안 보이는 문제가 그대로 남기 때문.)
2. **같은 단어가 서로 다른 오류 유형으로 중복 표시됨**: "오리가 강가에서 놀아요"에서 '놀아요'가 `단어 발음 오류`와 `불필요한 말 추가` 두 태그로 동시에 뜸. 사용자가 "'놀아요'는 원래 문장에 있는 단어라 불필요한 추가가 아니라 발음 오류가 맞는 것 같다"고 지적함 → Azure의 미스큐(EnableMiscue) 정렬이 목표 문장에 실제로 있는 단어를 `Insertion`으로 잘못 표시하는 경우가 있다고 보고, **`Insertion`으로 분류된 단어가 목표 문장 안에 실제로 있으면 `Mispronunciation`으로 재분류**하도록 후처리를 추가함. 같은 단어·같은 오류 유형이 중복으로 잡히는 것도 화면에 한 번만 보이도록 정리함.
3. node로 (실제 사례와 같은 구성의) 5가지 시나리오를 시뮬레이션해서 확인함: ① "놀아요" 중복 표시가 발음 오류 하나로 합쳐지는지, ② 76점 문항이 69점으로 낮아지고 오류가 보이는지, ③ 86점 문항도 동일하게 처리되는지, ④ 오류가 전혀 없는 99점 문항은 그대로 유지되는지, ⑤ Azure 응답이 `PronunciationAssessment`로 한 번 더 감싸진 형태로 와도 `wordAssessmentField` 헬퍼가 문제없이 읽는지 - 5가지 모두 의도대로 동작함을 확인함.
   - **브라우저 실사용 재확인 필요** — 특히 69점 처리 기준이 실제 채점 감각과 맞는지(너무 가혹하지 않은지), Insertion→Mispronunciation 재분류가 다른 문항에서도 자연스러운지 다음 세션에서 확인.

### 열두 번째 라운드 - 결과 문구 중복 + "도달" 판정 기준 개선 (2026-09-03, 위 수정 실사용 직후)

1. **결과 화면 문구 중복**: "조금 더 또박또박 읽어볼까요?"(헤드라인) 바로 아래 "받침이나 소리의 일부가 정확하지 않았어요. 또박또박 다시 말해보세요."(이유)가 이어지면서 "또박또박"이 두 번 겹쳐 보인다는 지적 → `explainLowScore`의 마지막 기본 문구에서 "또박또박"을 빼고 "다시 한번 말해보세요"로 바꿈.
2. **"도달" 판정이 평균만 봐서 실제로는 대부분 틀렸는데도 도달로 뜸**: 문장 단계에서 5문항 중 1문항만 맞았는데(97/60/69/69/69점) 평균이 73점이라 "도달"로 표시됨. 원인은 `renderHistory`(결과 확인 화면)와 `renderReport`(리포트 화면) 둘 다 그 단계 최근 시도의 **평균 점수만** 보고 70점 이상이면 도달로 판정하고 있었기 때문 — 문항별 정답 여부는 전혀 안 봄. → **`isSessionPassed(session)` 함수를 새로 만들어** "평균 70점 이상"과 "실제로 맞은 문항 비율 70% 이상" **둘 다** 만족해야 도달로 인정하도록 바꾸고, 두 화면 모두 이 함수를 쓰도록 통일함. (이 기준은 문장/문단뿐 아니라 1~6단계에도 똑같이 적용된다 - 원래 있던 같은 종류의 문제라서 전체에 일관되게 고쳤음. 다만 6단계까지의 "해득/보충/미해득" 종합 판정(`achievementTier`)은 이번엔 안 건드림 - 거긴 여전히 단계별 평균만 보는데, 필요하면 나중에 같은 방식으로 손볼 수 있음.)
3. **위 2번을 고치면서 "지도 방안이 안 뜨는" 문제도 같이 해결됨**: 리포트의 "4. 추천 학생 지도 방안"은 `stageRows`에서 `passed`가 false인 단계만 골라 보여주는데, 그동안 문장 단계가 평균 기준으로 `passed: true`로 잘못 판정되고 있어서 오답이 4개나 있어도 지도 방안 목록에서 아예 빠지고 있었다. `isSessionPassed`로 판정 기준을 고치자 이 단계도 자연스럽게 "보완 필요"로 잡혀 지도 방안이 뜨게 됨 - 별도 수정 불필요.
- node로 스크린샷과 같은 구성(97/60/69/69/69점, 1/5 합격)을 시뮬레이션해서 새 기준으로는 "미도달"이 나오는지, 반대로 4/5가 정상 통과한 케이스는 그대로 "도달"이 나오는지 둘 다 확인함.
- **브라우저 실사용 재확인 필요** — 특히 1~6단계에서도 이 기준 변경으로 도달/미도달이 뒤바뀌는 기존 기록이 있는지, 그게 자연스러운지 확인.

### 열세 번째 라운드 - 리포트 "수준" 판정도 같은 문제 발견 (2026-09-03, 위 수정 실사용 직후)

리포트를 실제로 뽑아보니, 문장 단계가 "미도달"인데 "2. 종합 결과"의 "수준" 칸은 "잘함"으로 뜸(전체 평균 73점만 보고 판정) → 바로 아래 "종합의견"은 이미 `failedStages`를 정확히 보고 "보충이 필요한 항목: 7. 문장"이라고 안내하고 있어서, 같은 리포트 한 장 안에서 "수준: 잘함"과 "보충 필요"가 서로 모순되게 보이는 문제였음. `achievementLevel`이 열두 번째 라운드에서 고친 `isSessionPassed`(도달 여부)를 전혀 안 쓰고 여전히 평균(`overallAvg`)만 보고 있던 게 원인.
- `achievementLevel(avg, hasFailedStage)`로 시그니처를 바꿔서, 시도한 단계 중 미도달(`failedStages`)이 하나라도 있으면 평균이 아무리 높아도 "잘함/매우 잘함"으로는 표시하지 않고 "보통"(평균 50점 이상) 또는 "미흡"(50점 미만)으로만 나오게 함.
- node로 확인: 평균 73점+미도달 있음 → "보통"(예전엔 "잘함"), 평균 81점+미도달 없음 → "잘함"(정상 케이스는 그대로), 평균 40점+미도달 있음 → "미흡".
- **브라우저 실사용 재확인 필요** — 리포트를 실제로 뽑아서 "수준"과 "종합의견"이 이제 서로 모순 없이 일치하는지 확인.

### 열네 번째 라운드 - 판정 체계 전체 종합 검토 (2026-09-03, 사용자 요청)

지금까지 문제가 발견될 때마다 그때그때 고쳤던 "도달/미도달, 점수 평균, 수준" 판정 로직을 사용자가 한 번에 종합 검토해달라고 요청함. 리포트·결과 확인·학생 목록·학급통계에서 쓰는 판정 기준을 전부 훑어봄.

**가장 중요한 문제 발견 및 수정**: 이 앱에서 가장 헤드라인이 되는 지표인 **`achievementTier`(해득/보충/미해득/미검사 판정)가 열두 번째 라운드에서 고친 `isSessionPassed` 기준(평균 70점 이상 + 실제 정답 비율 70% 이상)을 안 쓰고, 여전히 예전처럼 `stageAvgs`(단계별 평균)만 보고 있었음**. 즉 "도달" 배지와 리포트 "수준"은 고쳤는데, 정작 학생 목록·학급통계에 뜨는 해득수준 배지는 여전히 예전 버그(평균만 보고 판정 → 5문항 중 1개만 맞아도 평균이 70 넘으면 통과로 셈)를 갖고 있었던 것.
- `computeStudentStats`가 `stageAvgs`(단계별 평균, 학급통계 막대그래프처럼 순수 평균이 필요한 곳에 계속 씀)와 별도로 **`stagePassed`**(단계별로 `isSessionPassed` 기준을 적용한 통과 여부, null/true/false)를 새로 계산하도록 확장함.
- `achievementTier`가 `stageAvgs` 대신 `stagePassed`를 보고 해득(6단계 전부 통과)/미해득(모음·자음 중 하나라도 미통과)/보충(그 외)을 판정하도록 바꿈.
- node로 4가지 시나리오 시뮬레이션: ① 6단계 정상 통과(4~5/5) → 해득, ② 모음 단계가 "1/5만 합격, 평균은 70 넘음"인 경우(예전 버그면 해득에 낄 수 있었음) → 미해득으로 정확히 걸러짐, ③ 중간 단계(이중모음)만 미도달인 경우 → 보충, ④ 기록 없음 → 미검사. 4가지 모두 의도대로 나옴.

**그 외 검토했지만 이번엔 고치지 않은 것 (사용자 판단이 필요해 보임)**:
1. **문장/문단(7·8단계)의 69점 캡이 "정도"를 구분 못 함**: 단어 하나만 틀려도, 열 단어 중 여덟 개만 틀려도 똑같이 69점으로 깎임. 1~6단계는 "틀린 음절 비율"에 비례해서 40~69점 사이를 세분화하는데(`classifyMismatch`의 `raw = 100 * (exactCount + 0.3*jongDiffCount) / len` 방식), 7·8단계는 그런 비례 없이 "오류 있음=69점 고정"이라 성격이 다름. 오류 단어 비율에 비례해서 점수를 더 세분화할지 검토가 필요해 보임.
2. **판정 라벨이 4가지 층으로 겹쳐 있음**: ① 문항별 합격/불합격(70점) ② 단계별 도달/미도달(`isSessionPassed`) ③ 리포트 "수준"(매우잘함/잘함/보통/미흡, 1~8단계 전체 평균 기반) ④ 해득/보충/미해득/미검사(1~6단계만, `achievementTier`). ③과 ④는 서로 다른 계산(③은 시도한 모든 단계 포함 평균+실패단계 유무, ④는 1~6단계 통과 패턴)이라 같은 학생이라도 리포트 안에서 "수준: 보통"과 "해득수준: 보충"처럼 살짝 다른 표현이 동시에 뜰 수 있음 - 틀린 건 아니지만 교사가 헷갈릴 수 있어서, 나중에 라벨을 통일하거나 관계를 리포트에 한 줄로 설명해주면 좋을 듯.
3. **정답 비율 70%(5문항 기준 사실상 4/5 이상 요구) 기준이 적절한지**: 이 정도면 일반적인 "숙달 기준"(mastery, 보통 80% 안팎을 기준으로 삼는 경우가 많음)과 비슷한 수준이라 크게 무리는 없어 보이지만, 처음 정한 숫자라 실제로 몇 명 더 검사해보면서 너무 엄격하거나 헐거운지 감을 보는 게 좋을 것 같음.
- 브라우저 실사용 확인은 열두/열세 번째 라운드 항목과 함께 다음 세션에서 진행.

### 열다섯 번째 라운드 - 종합 검토 결과 1·2·3번 순서대로 조치 (2026-09-03)

열네 번째 라운드에서 찾은 3가지 항목을 사용자가 "위에서부터 고쳐보자"고 요청. 에이전트가 항목마다 추천안을 먼저 제시하고 그대로 진행함.

1. **[코드 수정] 7·8단계 69점 캡을 비례식으로 세분화**: 1~6단계가 이미 쓰는 "완전히 다름(0~29점) vs 일부만 다름(40~69점)" 2단 구조를 그대로 가져와서, 오류 단어 비율이 50% 이하면 40~69점 사이에서, 50%를 넘으면 0~39점 구간에서 비율에 비례해 점수가 매겨지도록 `assessRecording`의 compositeScoring 분기를 바꿈(`Math.min(accuracy, 69)` 고정 캡 삭제, `badRatio` 기반 `proportionalCap`으로 대체). node로 확인: 8단어 중 1개 틀림→62점, 3단어 중 1개 틀림→50점, 4단어 중 2개(절반) 틀림→40점, 4단어 전부 틀림→0점, 8단어 중 5개(과반 초과) 틀림→29점 - 문장이 길수록 단어 하나의 비중이 작아져 감점이 덜한 게 의도대로 반영됨.
2. **[코드 수정] 리포트 "수준"을 해득/보충/미해득/미검사로 통일**: 리포트 전용으로 따로 계산하던 `achievementLevel`(매우잘함/잘함/보통/미흡, 이미 두 번 패치했던 임시방편 성격)을 완전히 삭제하고, 학생 목록·학급통계와 완전히 같은 `achievementTier` + `levelBadgeHtml`(같은 색 배지)을 리포트의 "2. 종합 결과" 표에도 그대로 씀. 이제 한 학생을 두고 화면마다 다른 표현(예: "수준: 보통" vs "해득수준: 보충")이 동시에 뜨는 일이 없음.
3. **[검토만, 코드 변경 없음] 정답 비율 70% 기준 유지**: 세션 5문항 기준으로는 이미 사실상 "4/5(80%) 이상이어야 통과"인 구조라(3/5=60%는 70% 미달로 이미 떨어짐), 일반적인 숙달 기준(80% 안팎)과 맞아떨어져서 숫자를 더 조정할 이유가 없다고 판단함. 코드는 그대로 두고, 실사용해보면서 체감이 다르면 그때 조정하기로 함.
- **브라우저 실사용 재확인 필요** — 특히 1번(비례 캡)이 실제 문장 채점에서 체감상 적절한지.

### 열여섯 번째 라운드 - 세션 문항 수 5개 → 10개 (2026-09-03)

사용자가 한 세션당 문항 수를 5개에서 10개로 늘려달라고 요청. 확인해보니 1~6단계는 문항 은행이 10~18개라 문제없지만, **7단계(문장)는 8개, 8단계(짧은 문단)는 5개뿐**이라 그대로면 두 단계만 10개를 못 채우는 상황이었음 → 사용자에게 "7·8단계도 새로 채워 넣을지" 물었고, 채워달라는 답을 받음.
- `renderStageList`의 문항 추첨 로직(`shuffle(pool).slice(0, Math.min(5, pool.length))`)에서 `5`를 `10`으로 바꿈.
- **7단계에 2개 추가**: '토끼가 풀밭에서 뛰어놀아요', '새들이 나무 위에서 노래해요' (기존 8개와 같은 난이도 - 받침 있는 흔한 낱말, 짧은 주어+서술어 구조).
- **8단계에 5개 추가**: '학교에서 친구를 만나요. 함께 재미있게 놀아요.' / '엄마가 맛있는 빵을 구워요. 온 집안에 냄새가 좋아요.' / '비가 오는 날엔 우산을 써요. 빗소리가 참 좋아요.' / '동생과 함께 그림을 그려요. 알록달록 예쁜 그림이 완성돼요.' / '할머니 댁에 놀러 가요. 할머니가 반갑게 맞아주세요.' (기존 5개와 같은 두 문장짜리 구조, 일상 소재).
- node로 `stages` 배열을 실제로 읽어 각 단계 문항 수를 세어봄: 1.모음 10 / 2.자음 18 / 3.받침없는단어 16 / 4.이중모음 11 / 5.이중모음단어 10 / 6.대표받침 13 / 7.문장 10 / 8.짧은문단 10 - 전부 10개 이상 확인됨.
- 열두 번째 라운드에서 만든 `isSessionPassed`(정답 비율 70% 이상)는 고정된 5가 아니라 그때그때 실제로 푼 문항 수(`done.length`) 기준으로 계산하도록 이미 짜여 있어서, 문항 수를 10개로 늘려도 별도 수정 없이 그대로 잘 맞음 - 오히려 10문항이면 "70% 이상"이 7/10으로 정확히 맞아떨어져서, 5문항일 때 반올림 때문에 사실상 80%(4/5)로 동작하던 것보다 기준이 더 정확해짐.
- **참고**: 문항 수가 늘어난 만큼 검사 1회당 Azure 발음 평가 API 호출 횟수도 2배로 늚(문항 하나당 순수 인식+발음 채점 2번 호출). 이미 Azure Cost Management에 예산 알림을 설정해 둬서 당장 문제는 아니지만, 사용량이 늘었다는 점은 참고.
- **브라우저 실사용 재확인 필요** — 실제로 각 단계에서 10문항씩 나오는지, 새로 추가한 문장/문단이 자연스럽게 읽히는지 확인.

### 열일곱 번째 라운드 - 결과 화면 글자 크기 + 인식 소리 수동 정정 + 세션 상세 표 간소화 (2026-09-03)

1. **결과 화면(정답/오답 표시 화면)에서 문장/문단 글자가 너무 큼**: 문제 풀이 화면(`renderPractice`)에는 글자 수에 따라 줄어드는 `.long`/`.paragraph` 클래스가 있었는데, 정작 채점 결과를 보여주는 `renderResult`에는 이 클래스가 빠져 있어서 문장/문단도 64px 기본 크기 그대로 나오고 있었음(단순 누락 버그) → `renderResult`에도 같은 기준(`item.text.length > 20` → paragraph, `> 8` → long)의 `textSizeClass`를 추가함.
2. **"인식된 소리와 다름 (선생님 확인)" 기능 추가**: 사용자가 "Azure가 실제로 말한 소리와 전혀 다른 텍스트를 인식 결과로 내놓는 경우가 가끔 있는 것 같다"며, 기존 "잘 발음했어요(선생님 확인)" 버튼 옆에 선생님이 직접 타자로 인식된 소리를 고쳐 넣을 수 있는 기능을 요청함.
   - 오답 화면에 빨간 테두리의 "인식된 소리와 다름 (선생님 확인)" 버튼을 추가 → 누르면 그 아래 빨간 테두리 입력창(기존 인식 결과가 기본값으로 채워짐)과 "저장" 버튼이 펼쳐짐(`state.editingRecognized` 토글, `goToNextItem`에서 항상 false로 리셋).
   - **점수(accuracy/pass)는 건드리지 않고 `recognizedText`만 고침**: 다시 채점하려면 Azure에 재요청이 필요해서 범위 밖으로 두고, "기록을 정확하게 남기는" 용도로만 설계함(사용자에게 이 한계를 명확히 설명하고 진행).
   - 이 항목은 `feedback: '선생님이 인식된 소리를 수정함'`로 표시하고, `analyzeError`에 이 경우만 따로 처리하는 분기를 추가함 - 점수는 원래 자동채점 값 그대로인데 고친 텍스트로 일반 자모비교 로직을 그대로 돌리면 점수와 안 맞는 진단이 나올 수 있어서(예: 고친 텍스트가 목표와 정확히 같은데 점수는 여전히 0점), 새 태그 `'선생님이 인식 소리 수정'` 하나로만 표시되게 함. `ERROR_TAG_ADVICE`에도 이 태그의 설명을 추가하고, 학급통계 "오류 유형별 학생" 집계에서는 `'(예전 기록)'`과 같은 이유로 이 태그를 제외함(진짜 교육적 오류 유형이 아니라 데이터 상태 표시일 뿐이라서).
3. **세션 상세 표의 "오류 내용" 칸을 태그만 나오도록 간소화**: 태그 아래에 태그만으로 설명 안 되는 경우 문장으로 된 부연 설명(`it.feedback`)을 추가로 보여주던 걸(예: "따" 항목에 "말이 자연스럽게 이어지지 않았어요...") 없애서, 모든 행이 태그(+문장/문단은 단어별 오류)만 나오도록 통일함(행마다 길이가 들쭉날쭉해서 산만하다는 피드백). 이제 안 쓰이게 된 `GENERIC_FEEDBACK_TEXTS` 상수도 같이 삭제함(죽은 코드 정리).
- node로 `analyzeError`가 선생님 수정 케이스에서 정상적으로 새 태그 하나만 반환하는지 확인함.
- **브라우저 실사용 재확인 필요** — 특히 2번 기능을 실제로 눌러서 입력창이 잘 뜨고, 저장 후 세션 상세/리포트에 고친 텍스트가 잘 반영되는지 확인.

### 열여덟 번째 라운드 - 선생님이 점수도 직접 고칠 수 있게 확장 (2026-09-03)

바로 위 "인식된 소리와 다름" 기능이 점수는 못 건드리게 일부러 막아뒀는데, 사용자가 점수도 고칠 수 있어야 한다고 요청 + "임의로 숫자만 적게 하면 기준이 없어 들쭉날쭉해지니, 지금 채점 로직을 요약한 가이드라인을 같이 보여달라"고 요청함.
- 버튼 이름을 "인식된 소리와 다름 (선생님 확인)" → **"점수/인식된 소리 수정 (선생님 확인)"**으로 바꾸고, 펼쳐지는 패널에 "인식된 소리" 입력창 위에 **점수(0~100) 입력창**을 추가함.
- **`scoringGuidelineText(stage)` 함수 신설**: 그 단계가 `compositeScoring`(7·8단계)인지에 따라 실제 `assessRecording` 채점 로직을 그대로 옮겨 적은 안내 문구를 보여줌.
  - 1~6단계: "정확히 발음 = 90~100점 · 받침만 다름 = 40~69점 · 초성/중성 자체가 다름(완전히 다른 소리) = 0~29점"
  - 7·8단계: "자연스럽게 잘 읽음 = 70점 이상 · 틀린 단어 절반 이하 = 40~69점(비율에 비례) · 절반 초과 = 0~39점(비율에 비례)"
  - 이 문구는 코드에 있는 숫자를 그대로 옮겨 쓴 것이므로, 나중에 채점 로직(비례 캡 등)을 다시 바꿀 때 이 문구도 같이 확인해야 한다고 주석에 남겨둠 - 안 그러면 안내와 실제 채점이 어긋나게 됨.
- 저장 시 입력값 검증: 점수 입력칸을 비워두거나 숫자가 아니면 원래 점수로 되돌리고, 값이 있으면 0~100 범위로 잘라냄(clamp). node로 85/150(→100)/-5(→0)/abc(→원래값)/빈칸(→원래값) 케이스를 확인함.
- `feedback` 마커를 `'선생님이 인식된 소리를 수정함'` → **`'선생님이 직접 채점함'`**으로 통일(이제 점수도 같이 고치므로), `analyzeError`/`ERROR_TAG_ADVICE`/학급통계 필터의 태그명도 `'선생님이 직접 채점'`으로 맞춰서 함께 수정함.
- **브라우저 실사용 재확인 필요** — 패널이 잘 펼쳐지는지, 점수를 고쳐서 저장하면 세션 상세/리포트/학급통계에 새 점수·태그가 일관되게 반영되는지 확인.

### 열아홉 번째 라운드 - UI 디테일 정리 (2026-09-03)

사용자가 "이제 큰 구조는 손댈 것 없으니 디테일한 것들을 확인해보고 제안하라"고 요청. 전체 CSS와 인라인 스타일을 훑어서 발견한 것들을 정리해 제안함:
1. 회색 보조 텍스트 색이 `#999`/`#888`/`#666`/`#555`로 앱 곳곳에 다르게 흩어져 있음(앱 공통 변수는 `--text-muted` #6b7280 하나) - 특히 `#999`는 흰 배경 대비가 약해 가독성도 아쉬움.
2. 세션 상세 표의 점수 색(`#27ae60`/`#e74c3c`)이 앱 나머지가 쓰는 초록·빨강(`#16a34a`/`#dc2626`)과 미묘하게 다른 색이었음.
3. (제안했으나 보류) "점수/인식된 소리 수정" 패널의 이중 빨간 테두리 강조 완화.
4. (제안했으나 보류) 새 입력창 2개의 모서리 둥글기(4px)를 앱 기준(6px)에 맞춤.

**사용자가 1·2번만 승인**하여 그것만 진행함: 결과 화면의 "인식된 소리"/"이유" 문구, 세션 상세의 단어별 오류 줄, 채점 가이드라인 문구에 흩어져 있던 회색을 전부 `var(--text-muted)`로 통일하고, 세션 상세 표의 점수 색을 앱 공통 초록·빨강으로 맞춤. 3·4번(패널 테두리 이중 강조, 입력창 모서리)은 손대지 않음 - 나중에 필요하면 다시 제안 가능.
- 코드 전체에서 다른 회색·초록·빨강 잔재가 더 없는지 grep으로 재확인함.
- **브라우저 실사용 재확인 필요** — 특히 결과 화면과 세션 상세 화면에서 색이 자연스럽게 통일돼 보이는지.

### git push 전 코드 리뷰 (2026-09-03)

push 전 diff를 다시 훑어보며 2가지를 고침:

1. **주석 위치가 엉킴**: 로고 클릭 이벤트를 추가하면서 그 설명 주석이 `getSession()` 관련 기존 주석과 그 주석이 설명하는 실제 코드(IIFE) 사이에 끼어들어감 → 로고 클릭 코드+주석을 그 앞으로 옮겨서 원래 주석이 자기가 설명하는 코드 바로 위에 오도록 정리.
2. **죽은 코드**: 해득수준 판정을 평균 점수(`overallAvg`) 기준에서 단계별 배열(`stageAvgs`) 기준으로 바꾸면서, `computeStudentStats`가 계산해두던 `overallAvg` 값을 더 이상 아무도 읽지 않게 됨 → 계산과 반환 필드에서 제거.

`node --check`로 스크립트 문법 오류 없음을 재확인함.

## git push 전 코드 리뷰 (2026-09-02, 첫 배포 전)

[00번 가이드](00-프로젝트-에이전트-가이드.md)에 새로 정한 "git push 전 습관"에 따라, 오늘 쌓인 변경사항을 push하기 전에 `/code-review` 스킬로 자체 검토를 거침. 발견된 4가지를 모두 그 자리에서 고침:

1. **세션 상세 화면 오표시**: "모르겠어요"/"잘 발음했어요(선생님 확인)"으로 만들어진 항목(둘 다 recognizedText 필드가 아예 없음)이 "(예전 기록)"으로 잘못 표시되던 문제 → feedback 문구로 먼저 구분해서 "(모르겠음)"/"(선생님 확인)"으로 정확히 표시하도록 고침.
2. **Chart.js 로드 실패 대비 없음**: 엑셀 라이브러리(XLSX)는 `typeof XLSX === 'undefined'` 체크가 있는데 Chart.js는 없어서, CDN이 막힌 환경(오프라인 교실 등)에서 학급통계를 열면 흰 화면으로 깨지던 문제 → `chartAvailable` 체크를 추가해서, 그래프 라이브러리를 못 불러왔으면 각 차트 자리에 안내 문구를 대신 보여주도록 고침.
3. **자동 로그인 확인에 try/catch 없음**: 페이지가 열릴 때 `sb.auth.getSession()`이 실패하면(저장된 로그인 정보 손상 등) 어느 화면도 안 그려지고 완전히 빈 화면으로 멈추던 문제 → try/catch로 감싸서, 실패해도 로그인 화면으로 안전하게 넘어가도록 고침.
4. **학생/반/선생님 이름 미이스케이프**: 이름에 `<`, `>`, `&`, `"` 같은 문자가 들어있으면 화면이 깨지거나(리포트 표 등) 학생 이름 입력칸이 통째로 깨질 수 있던 문제(입력칸은 `value="..."` 속성 안에 그대로 넣고 있어서 특히 위험했음) → `escapeHtml()` 함수를 새로 만들어 학생 이름·반 이름·선생님 이름이 화면에 나가는 모든 지점에 적용함. (참고: Azure 인식 결과는 `normalizeHangul`이 한글 완성형 문자만 남기므로 애초에 위험한 문자가 나올 수 없어 이스케이프가 필요 없음 - 코드 리뷰에서도 이 지점은 제외하고 판단함.)

node 시뮬레이션으로 4가지 모두 고쳐졌는지 재확인함(Chart 미정의 상태에서 fallback 문구가 뜨는지, 이름에 `<script>` 태그를 넣어도 그대로 이스케이프되는지 등). 이 정리를 마친 뒤 커밋 + `git push` 진행.

### 다음 세션에서 확인할 것

1. **Supabase SQL Editor에서 `supabase/schema.sql` 실행했는지**, **Authentication에서 Confirm email을 껐는지** 먼저 확인 (안 했으면 로그인부터 안 됨).
2. 로그인 → 반 추가 → 학생 수 추가/엑셀 업로드 → 검사 시작 → 채점까지 실제 브라우저에서 전체 흐름 테스트 (제 환경에서 브라우저 조작을 직접 못 해서 아직 미검증).
3. 다른 컴퓨터(또는 시크릿 창)에서 같은 이름+비밀번호로 로그인했을 때 같은 반/학생이 보이는지 확인 (진짜 서버 저장이 되는지의 핵심 테스트).
4. 엑셀 업로드가 실제 `.xlsx` 파일로 잘 되는지.

## GitHub 저장소 (완료)

- 로컬 git 저장소 초기화 + 첫 커밋 완료 (`.env.local`은 `.gitignore`로 제외됨, 확인함).
- 사용자가 직접 https://github.com/tmdry1999-lgtm/hangeul.git 저장소를 만들고 URL을 전달함 → `git remote add origin` + `git push -u origin master`로 연결/업로드 완료.
- **다음에 할 일**: Vercel과 이 GitHub 저장소 연결(Vercel 대시보드에서 Import) + Vercel 환경 변수에 실제 Azure 키(`AZURE_SPEECH_KEY`, `AZURE_SPEECH_REGION`) 등록. 이걸 하면 이후 push할 때마다 자동 배포됨 (다른 컴퓨터에서도 GitHub clone/pull로 이어서 작업 가능).
- 학생 관리 기능 추가로 파일이 꽤 커져서, 다음에 push할 때 사용자에게 먼저 알리고 진행할 것 ([00번 가이드](00-프로젝트-에이전트-가이드.md) "큰 변경 전 확인" 규칙).

## 다음 할 일

1. (다음 세션 시작 시) 사용자가 브라우저에서 http://localhost:3000 새로고침 후, 방금 고친 모범 발음(SunHi 음성)과 마이크 재클릭 취소가 잘 되는지 확인
2. 위 "알려진 문제"의 짧은 음절 인식 한계를 1·2단계 위주로 더 테스트해보고, 필요하면 통과 점수 기준을 조정
3. 정상 동작 확인 후 git 저장소 초기화 + GitHub 저장소 생성 + 최초 push (진행 전 사용자에게 확인)
4. Vercel과 GitHub 저장소 연결, Vercel 환경 변수에 실제 Azure 키 등록 (로컬 CLI 로그인 문제와 무관하게 대시보드에서 진행 가능)

## 개발 환경 확인 완료

- Node.js v24, npm 11 설치되어 있음.
- Vercel CLI는 별도 설치 없이 `npx vercel`로 사용 (전역 설치 안 함).
- Vercel 계정 있음. Azure 계정: 대학원 계정으로 Azure for Students 인증 완료.
- 아직 git 저장소 아님 — **GitHub 저장소를 새로 만들어 연결하기로 결정함** (컴퓨터 두 대 동기화 목적).
